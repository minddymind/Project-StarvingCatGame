import java.awt.AWTEvent;

public class Messenger {
    public static final int UPDATE = AWTEvent.RESERVED_ID_MAX + 1;   // send message to update method in Class CatBoard
    public static final int COLTEST = AWTEvent.RESERVED_ID_MAX + 2;
}